addons:
  firefox: "45.0"
language: ruby
rvm:
 - 2.3.0
before_script:
  - export DISPLAY=:99.0
  - sh -e /etc/init.d/xvfb start
script:
  - bin/rails db:setup RAILS_ENV=test
  - bundle exec rspec spec
